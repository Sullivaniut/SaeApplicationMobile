import 'package:flutter/material.dart';

class AnnonceUI extends StatelessWidget {
  const AnnonceUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Annonce'),
      ),
    );
  }
}